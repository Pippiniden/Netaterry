# 公開・運用ガイド（運営者向け）

Netaterry を GitHub Pages で公開し、必要に応じて Google ドライブ同期を有効にするための手順です。
機能そのものの説明は [README.md](README.md) を参照してください。

## 公開手順（GitHub Pages）

1. GitHub で新しいリポジトリを作り、このフォルダの中身をすべてアップロードします
   （`index.html` がリポジトリ直下に来るように。`tests/` フォルダと `DEPLOY.md` はアップロード任意です）。
2. リポジトリの **Settings → Pages** で、Source を「Deploy from a branch」、Branch を `main` / `/(root)` にして保存します。
3. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で開けます。

この時点で、Google ドライブ同期以外のすべての機能が使えます。

## Google ドライブ同期の設定（任意）

### 公開して大丈夫？（セキュリティについて）

`config.js` に書く「OAuth クライアント ID」は、GitHub のリポジトリを Public にする以上、誰でも見える状態になります。ですが、これは Google の設計上そもそも公開される前提の値で、それ自体が漏れても悪用はできません（本当の秘密情報である「クライアントシークレット」は、このアプリの認証方式では一切使いません）。

ただし、クライアント ID が知られると、**このアプリを開いた人なら誰でも「Googleでログイン」ボタンを押せてしまいます**。ログインした人がアクセスできるのはその人自身の Google Drive の専用領域だけなので、あなたのデータが盗まれることはありませんが、意図しない人に使われるのは避けたいところです。これを防ぐには、下記の手順3にある **OAuth 同意画面を「テスト」ステータスのまま**にしてください。テストユーザーとして登録した自分の Google アカウント以外はログインできなくなり、事実上あなた専用の同期機能になります。「本番環境」に公開しない限り、この制限はそのまま有効です。

1. [Google Cloud Console](https://console.cloud.google.com/) でプロジェクトを作成します。
2. **APIとサービス → ライブラリ** で「Google Drive API」を有効にします。
3. **APIとサービス → OAuth 同意画面** を設定します。
   - ユーザーの種類：外部
   - スコープに `.../auth/drive.appdata` を追加
   - 公開ステータスが「テスト」の間は、**テストユーザー**に自分の Google アカウントを追加してください
     （テスト中は約7日ごとに再同意が必要になることがあります）
   - **他人にログインボタンを使われたくない場合は、公開ステータスを「テスト」のまま維持してください。**
     テストユーザー以外は Google 側でログインを拒否されるため、事実上あなた専用になります。
     「本番環境」に公開すると誰でもログインできるようになります
     （`drive.appdata` は機密性の高いスコープではないため、公開自体は通常審査なしで通ります）
4. **APIとサービス → 認証情報 → 認証情報を作成 → OAuth クライアント ID** を選び、
   - アプリケーションの種類：ウェブアプリケーション
   - 承認済みの JavaScript 生成元：`https://<ユーザー名>.github.io`（末尾のスラッシュやリポジトリ名は不要）
5. 表示されたクライアント ID を `config.js` の `GOOGLE_CLIENT_ID` に貼り付けて、リポジトリに反映します。

アプリ右上の「ローカル」ボタン（またはメニュー →「Googleでログイン」）からログインすると同期が始まります。

## アプリを更新するとき

新しい版のファイルでリポジトリを上書きします。

- **`config.js` は上書きしない**でください（自分のクライアント ID を書いてある場合。上書きすると同期できなくなります）
- それ以外のファイルはすべて上書きして問題ありません。新しく増えたファイルもそのまま追加します
- `sw.js` の `VERSION`（例 `v1.0.0` → `v1.0.1`）が上がっていることを確認してください。上がっていないと各端末に新しい版が届きません

開いている端末に「更新があります（再読み込み）」が表示され、押したときだけ新しい版に切り替わります。

自分でファイルを追加した場合は `sw.js` の `ASSETS` にも追記してください。

### 旧アプリ名（ノベルメモ）から更新する場合

アプリ名の変更にあわせて、端末内の保存先とドライブ上のファイル名も変わりましたが、旧データは自動で引き継がれます。

- 端末内のデータ：新しい版を初めて開いたとき、旧版のデータ（ノード・テンプレート・設定・同期情報）を写します。旧版のデータ自体は消さずに残します
- Google ドライブ：旧ファイル名 `novelmemo.json` が見つかれば、そのファイルを使い続けます
- 旧版で書き出した往復用 Markdown・設定ファイルもそのまま読み込めます

## ファイル構成

```
index.html              画面の骨組み
style.css               スタイル
config.js               Google クライアントID
sw.js                   Service Worker（オフライン対応）
manifest.webmanifest    PWA 設定
icons/                  アイコン
js/
  app.js                起動・画面全体の制御
  store.js              状態管理・Undo/Redo・ノード操作
  db.js                 IndexedDB
  merge.js              データ移行・ノード単位マージ・ゴミ箱の掃除
  sync.js / drive.js    Google ドライブ同期・ログイン
  search.js             検索・置換
  tags.js               タグの正規化・集計
  markdown.js           エクスポート整形・Markdown 相互変換
  richtext.js           閲覧モードの本文記法（ルビ・傍点・Markdown）の変換
  util.js               共通処理
  ui/                   画面部品（ツリー・エディタ・検索・閲覧・各種ダイアログ）
tests/unit.test.mjs     ロジックのテスト（node --test tests/unit.test.mjs。公開時は不要）
```

## データの保存形式

ドライブ上の `netaterry.json`（バックアップ JSON も同じ形式）:

```json
{
  "schemaVersion": 2,
  "nodes": [ ... ],
  "templates": [ ... ],
  "globalNote": { "text": "...", "updatedAt": 0 }
}
```

`schemaVersion` の履歴:

| 版 | 変更 |
| --- | --- |
| 1 | 初版（nodes / templates） |
| 2 | 共通メモ `globalNote` を追加（v1 のデータは読み込み時に空の共通メモを補って移行） |

古い版のアプリは新しい `schemaVersion` のデータを読み込まず、「アプリを更新してください」と表示して同期を止めます（データを壊さないため）。
スキーマを変えたときは、`sw.js` の `VERSION` も上げて各端末に更新を届けてください。

表示設定・テーマ・絞り込み状態・検索履歴は端末ごとの保存で、同期しません（設定画面から設定ファイルとして書き出し・読み込みできます）。
